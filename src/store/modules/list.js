// 这里定义初始值
const state = {
  userinfo: 'jaja2'
};
const mutations = {
};
// 事件触发后的逻辑操作
// 参数为事件函数
const actions = {
};
const getters = {
};
export default{
  state,
  mutations,
  actions,
  getters
};
